[build]
target = "thumbv7em-none-eabihf"

[target.thumbv7em-none-eabihf]
runner = "probe-rs run --chip nRF52833_xxAA --protocol swd"
rustflags = [
  "-C", "linker=flip-link",
  "-C", "link-arg=-Tlink.x",
  "-C", "link-arg=-Tdefmt.x",
]

# Show defmt debug/info/warn/error output by default; override per-shell with
# `DEFMT_LOG=trace cargo run --release`, etc.
[env]
DEFMT_LOG = "debug"
